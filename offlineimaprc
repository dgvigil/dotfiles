[general]
ui = TTY.TTYUI
accounts = SANDIA

[Account SANDIA]
localrepository = Local
remoterepository = EXCHANGE
#status_backend = sqlite
postsynchook = notmuch new

[Repository Local]
type = Maildir
localfolders = ~/Mail/

[Repository EXCHANGE]
maxconnections = 3
type = IMAP
remotehost = mail.sandia.gov
remoteuser = dgvigi
remotepass = 
auth_mechanisms = PLAIN, LOGIN
remoteport = 993
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
realdelete = yes
